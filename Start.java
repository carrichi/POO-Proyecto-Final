/**
 * Esta es la clase que realizara la ejecucion de todo el sistema de administracion.
 */
public class Start {
    /**
     * Metodo que realiza el comienzo de todo el programa.
     * @param args Indica los argumentos que puede recibir desde la consola de comandos.
     */
    public static void main(String[] args) {
        /*
        Al inicial el programa se requiere realizar un LOGIN y buscar si los datos son correctos.
        */
        
        /*
            Despues de que se haya registrado, es necesario mostrarle un menu me opciones segun el usuario que haya
            iniciado sesion.

            Este sera un ciclo que no terminara hasta que el usuario actual decida cerrar su sesión.
        */

        boolean exit = false;
        while ( ! exit ) {


            
            exit = true;
        }

    }
}